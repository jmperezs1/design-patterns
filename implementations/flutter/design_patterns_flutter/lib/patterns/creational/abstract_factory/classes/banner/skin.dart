import 'package:flutter/material.dart';

class Skin {
  final Color container, ring, accent, iconBg;
  final String defaultTitle;
  Skin(this.container, this.ring, this.accent, this.iconBg, this.defaultTitle);
}
